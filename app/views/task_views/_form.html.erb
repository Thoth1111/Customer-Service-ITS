<%= form_with(model: task_view) do |form| %>
  <% if task_view.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(task_view.errors.count, "error") %> prohibited this task_view from being saved:</h2>

      <ul>
        <% task_view.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
