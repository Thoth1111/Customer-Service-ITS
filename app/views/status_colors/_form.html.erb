<%= form_with(model: status_color) do |form| %>
  <% if status_color.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(status_color.errors.count, "error") %> prohibited this status_color from being saved:</h2>

      <ul>
        <% status_color.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
