<%= form_with(model: squad) do |form| %>
  <% if squad.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(squad.errors.count, "error") %> prohibited this squad from being saved:</h2>

      <ul>
        <% squad.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
